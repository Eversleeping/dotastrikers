function OnRefereeAttacked( keys )
	DotaStrikers:OnRefereeAttacked(keys)
end

function on_powershot_succeeded( keys )
	DotaStrikers:on_powershot_succeeded(keys)
end

function throw_ball( keys )
	DotaStrikers:throw_ball(keys)
end

function surge( keys )
	DotaStrikers:surge(keys)
end

function surge_break( keys )
	DotaStrikers:surge_break(keys)
end

function pull( keys )
	DotaStrikers:pull(keys)
end

function pull_break( keys )
	DotaStrikers:pull_break(keys)
end

function ninja_jump( keys )
	DotaStrikers:ninja_jump(keys)
end

function text_particle( keys )
	DotaStrikers:text_particle(keys)
end

function slam( keys )
	DotaStrikers:slam(keys)
end

function ninja_invis_sprint( keys )
	DotaStrikers:ninja_invis_sprint(keys)
end

function black_hole( keys )
	DotaStrikers:black_hole(keys)
end

function tackle( keys )
	DotaStrikers:tackle(keys)
end